import { Component } from '@angular/core';

import { Shell } from '@core/layout/shell/shell';

@Component({
  selector: 'sv-root',
  imports: [Shell],
  templateUrl: './app.html',
})
export class App {}
